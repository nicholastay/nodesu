'use strict';

/**
 * Enum for the gamemodes.
 * @memberof module:Enums
 * @readonly
 * @enum {Number}
 */
const Mode = {
    /* Limit results to osu! game mode specific. */
    osu: 0,
    taiko: 1,
    ctb: 2,
    mania: 3
};

module.exports = Mode;