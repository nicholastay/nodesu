/**
 * These are read-only data helpers when you specify arguments elsewhere. They are exposed to the main nodesu export (require('nodesu').enum).
 * In these docs pages, they should be listed under "Global" below.
 * @module Enums
 */