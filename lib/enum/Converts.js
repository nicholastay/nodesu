'use strict';

/**
 * Enum if converts should be returned (for beatmap queries)
 * @memberof module:Enums
 * @readonly
 * @enum {Number}
 */
const Converts = {
    include: 1,
    exclude: 0
};

module.exports = Converts;